# colab_pipeline — full complete pipeline

This folder is everything you need to run the **whole X-RAG pipeline** on Google Colab
from a single notebook. It contains the runtime `.py` code plus one master notebook.
